{% extends 'base.html.twig' %}

{% block body %}

    <h1>To Do</h1>

    <ul>
        {% for todo in todos %}
            <li>{{ todo.name }} (<a href="/tasks/{{ todo.id }}/complete">complete</a>)</li>
        {% endfor %}
    </ul>

{% endblock %}